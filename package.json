{
  "name": "javascript-course-master",
  "version": "1.0.0",
  "description": "",
  "main": "lesson.js",
  "scripts": {
    "precompress": "{{ executes BEFORE the `compress` script }}",
    "compress": "{{ run command to compress files }}",
    "postcompress": "{{ executes AFTER `compress` script }}"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ggarunchik/javascript-course-master.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ggarunchik/javascript-course-master/issues"
  },
  "homepage": "https://github.com/ggarunchik/javascript-course-master#readme",
  "devDependencies": {
    "eslint": "^8.13.0"
  },
  "dependencies": {
    "inquirer": "^8.2.2",
    "prompt-sync": "^4.2.0"
  }
}